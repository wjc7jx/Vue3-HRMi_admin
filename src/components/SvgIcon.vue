<template>
    <svg :style="{ width, height }">
        <use :href="prefix + name" :fill="color"></use>
    </svg>
</template>

<script setup>
import { defineProps } from 'vue';

defineProps({
    prefix: {
        type: String,
        default: "#icon-"
    },
    name: String,
    color: {
        type: String,
        default: "#ffffff"
    },
    width: {
        type: String,
        default: "16px"
    },
    height: {
        type: String,
        default: "16px"
    }
});
</script>